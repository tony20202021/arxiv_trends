[project]
name = "arxiv-trends-pipeline"
version = "0.1.0"
requires-python = ">=3.10"
description = "arXiv abstract keyword trends by domain -> weekly counts in MongoDB -> plots"
readme = "README.md"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend", "."]
