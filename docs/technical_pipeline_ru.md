config
## 1) Domains config
- `config/domains.json`: список доменов и arXiv search_query (например `cat:cs.LG`)

---

## Константы
Все константы (год, top-10, 10 недель) находятся в одном месте:
- `config/constants.py`


---
управление сервисами
зарегистрировать как сервисы
сделать скрипты для запуска-перезапуска
фронт и бэк - сделать авто-обновление при изменении файлов в каталогах

БД
Хранение в MongoDB
- `pymongo` bulk upsert: `(domain, week_start, keyword)` → `$inc: count`
- это и есть “сырые” агрегаты: неделя/слово/количество
БД будет на другом сервере в интернете

## Хранение данных
Сырые данные (неделя, ключевое слово, количество) хранятся в MongoDB:
- коллекция: `weekly_keyword_counts`
- поля: `domain`, `week_start`, `keyword`, `count`
TODO дописать остальные таблицы БД


/frontend
на другом сервере в интернете (там где БД)
разделить на каталоги
view
    telegram-bot
    aiogram
    одна команда /start - выводит краткое описание
    одна кнопка "получить свежие тренды" - выводит графики за самую свежую неделю из БД
    НЕ ЗАВИСИТ ОТ ДАННЫх
    чтобы можно было легко поменять на другой бот
    
model
    вся работа с данными
    готовит графики на основе данных из БД
    - `pandas`, `numpy`, `matplotlib` - выбрать что лучше подходит


# backend
работает на отдельном сервере с доступом к ЛЛМ
сервис всегда запущен
обновление по расписанию раз в неделю
проверка какие именно недели не заполнены в БД
если недоступен архив ждать с разумными тайм-аутами (1 час)
если недоступны ЛЛМ - ждать с разумными тайм-аутами (10 мин)
## 2) Получение статей
- `feedparser` + `requests`: arXiv API (Atom) → список статей за период
- для каждой статьи: скачивание HTML `/abs/<id>`

## 3) Извлечение abstract
- `beautifulsoup4` + `lxml`: `blockquote.abstract` → текст abstract

тексты абстрактов - временно сохранять во временном каталоге
ограничение на максимальное количество временных абстрактов

## 4) Извлечение ключевых слов
TODO переделать на вызов внешней ЛЛМ по API
данные для подключения - брать из внешнего env-файла
пароль для подключения - запрашивать  у пользователя при старте сервиса, нигде не хранить
на вход брать абстракты из временных файлов

## 5) Агрегация по неделям
- парсим дату публикации из Atom
- bucket по ISO-неделе (понедельник = начало недели)
- суммируем частоты токенов по неделе

## 7) подсчет данных для  графиков
На основе данных MongoDB за последние 52 недели:
1) **Top-10 popular (last week)**: топ по последней неделе
2) **Top-10 growing (last 10 weeks)**: топ по наклону (slope) линейной регрессии на окне 10 недель
TODO в бэкенде графики не рисовать, сохранить аггрегированные данные в БД в других таблицах - потом будет доставаться другим фронтендом

подумать как оптимальнее - может разделить бэк на 3 разных сервиса - 
сохранение с архива
ЛЛМ
агрегация


